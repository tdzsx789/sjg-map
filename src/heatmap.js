import * as maptalks from "maptalks";

class Heatmap {
    constructor(option, layer) {
        const {
            heatmap: {

            }
        } = option;

        console.log('tttt')
    }
}

export default Heatmap;